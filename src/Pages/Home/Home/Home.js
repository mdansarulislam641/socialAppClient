import React, { useContext } from 'react';
import AddPost from '../AddPost';

const Home = () => {
    return (
        <div>
            <AddPost></AddPost>
        </div>
    );
};

export default Home;